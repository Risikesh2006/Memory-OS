import LoginPage from "@/components/ui/animated-characters-login-page";

export const metadata = {
  title: "Login — Legacy OS",
  description: "Sign in to your Legacy OS account.",
};

export default function Page() {
  return <LoginPage />;
}
